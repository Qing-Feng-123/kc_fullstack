/* ============================================================
   resources.js — 资源页逻辑（占位）
   后端 kc-query-resources 接口就绪后：
     1. 在 api.js 的 ENDPOINTS 登记并启用 KC_API.getResources()
     2. 在这里调用并渲染，样式复用 .card / .summary-item 等
   ============================================================ */

// 示例骨架（接口就绪后取消注释并按需修改）：
// (async () => {
//   try {
//     const data = await KC_API.getResources();
//     document.getElementById('resContent').textContent = JSON.stringify(data);
//   } catch (e) {
//     document.getElementById('resContent').textContent = '受信失敗: ' + e.message;
//   }
// })();
